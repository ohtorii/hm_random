/*秀丸エディタから乱数列を生成するマクロ

「符号無し３２ビット」版。

*/
$s = input("生成する乱数の個数を指定して下さい","10");
if(0==strlen($s)){
	endmacro;
}
#num = val($s);
if(#num<=0){
	endmacro;
}
execmacro currentmacrodirectory+"\\hm_random_main.mac", "32", $s, "%d\\n";
